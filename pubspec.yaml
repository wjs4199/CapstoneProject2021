name: giveandtake
description: 21-1 MAD Team Project
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core : ^1.2.0
  cloud_firestore: ^2.2.0
  firebase_auth: ^1.2.0
  geolocator: ^7.0.3
  firebase_storage: ^8.0.5
  google_fonts: ^2.0.0
  google_sign_in: ^5.0.3
  provider: ^5.0.0
  cupertino_icons: ^1.0.2
  image_picker: ^0.8.0+1
  url_launcher: ^6.0.6
  fl_chart: ^0.36.1 # 새로운 api 사용(1) flutter chart
  flutter_sticky_header: ^0.6.0 # 새로운 api 사용(2) flutter sticky header
  google_maps_flutter: ^2.0.6 # 새로운 api 사용 (3) google maps flutter
  http: ^0.13.1
  json_serializable: ^4.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0
  build_runner: ^2.0.4

flutter:
  uses-material-design: true
  assets:
    - assets/hgu.png
    - assets/logo.png
    - assets/userDefault.png

  fonts:
    - family: Pacifico
      fonts:
        - asset: fonts/Pacifico-Regular.ttf

    - family: Source Sans Pro
      fonts:
        - asset: fonts/SourceSansPro-Regular.ttf

    - family: NanumBarunGothic
      fonts:
        - asset: fonts/NanumBarunGothic.ttf

    - family: NanumSquareRoundR
      fonts:
        - asset: fonts/NanumSquareRoundR.ttf
